---
- name: test my new module
  hosts: kubernetes
  tasks:
  - name: run the new module
    fault_injection_test:
      name: 'hello'
      new: true
    register: testout
  - name: dump test output
    debug:
      msg: '{{ testout }}'
